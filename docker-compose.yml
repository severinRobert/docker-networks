version: "3.3"
services:
  bob:
    image: alpine
    command: ash -c "apk update && apk upgrade && apk add busybox-extras && tail -f /dev/null"
    container_name: bob
  alice:
    image: alpine
    command: ash -c "apk update && apk upgrade && apk add busybox-extras && tail -f /dev/null"
    container_name: alice
